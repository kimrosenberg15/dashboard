# Claude Code — Flock Collateral

This repo builds a custom pitch-card PDF generator for Flock Safety.

**Before doing anything, read `PROMPT.md`** at the repo root. It points to the context files (`context/STRATEGY.md`, `context/DECISIONS.md`, `context/BRAND.md`, `context/PEOPLE.md`) and the canonical spec (`markup/field_specs.csv`).

Always read those files at session start. They're short.
